# AGENTS.md

本文件约束 Codex / Agent 在本仓库中的协作方式、修改边界和验证要求。

项目当前版本、功能状态和发布信息以仓库实际文件为准，不在本文件维护。

## 信息来源与工作模式

修改前必须阅读任务相关源码、配置和文档，不根据提示词或文件名猜实现。

AstrBot、QQ 官方机器人 API、插件市场规范等可能变化的信息，优先查询当前官方文档或源码；有明确目标版本时同时核对该版本。

无法确认的实现、兼容性或验证结果列入：

`待人工确认项`

### Plan 阶段

提示词要求“计划阶段 / Plan Only / 只分析不修改”时：

- 只阅读、搜索、分析和制定计划
- 不修改文件
- 不 commit
- 不创建 Tag / Release
- 不运行会改写仓库的自动修复或格式化命令

默认输出：

1. 任务理解
2. 当前实现与相关文件
3. 官方 API / 文档结论（如适用）
4. 原因或实现约束
5. 实施步骤
6. 预计修改文件
7. 验证方案
8. 风险
9. 待人工确认项

只有收到明确实施提示词后才能修改仓库。

### Implementation 阶段

- 先运行 `git status --short`
- 严格按已批准计划实施
- 只修改必要文件
- 不做计划外重构、格式化、依赖升级或发布调整
- 实际仓库与计划前提冲突时，先报告差异，不静默扩大范围

---

## 项目原则

优先级：

**简单 > 稳定 > 易维护 > 功能数量**

默认：

- 优先最小修改
- Python 标准库能解决时不新增第三方依赖
- 不提前引入数据库、Redis、ORM、Service、Repository 或独立后端
- 不为简单逻辑增加无意义抽象
- 未验证内容不能写成已通过

固定标识，除非任务明确要求重命名：

| 标识 | 固定值 |
| - | - |
| 插件内部名 | `astrbot_plugin_music_guess` |
| 显示名 | `音游开字母` |
| GitHub 仓库 | `Ice2974/astrbot_plugin_music_guess` |
| 作者 | `Ice2974` |

`ha_na` 只是某个 Bot 实例名，不属于插件公共身份。

版本号和 AstrBot 兼容范围以 `metadata.yaml` 为准。

---

## 架构与依赖边界

核心通常保持为：

- `main.py`：游戏逻辑和 AstrBot 事件处理
- `songs.txt`：简单曲库
- `metadata.yaml`：插件元数据
- `_conf_schema.json`：如存在配置
- `README.md`：用户文档
- `SOURCES.md`：第三方曲库 / 数据来源
- `LICENSE`：项目许可证

除非任务证明必要，不新增数据库、后端服务、多层业务架构或大量小模块。

如果 `main.py` 已明显难维护，可在 Plan 阶段提出最小拆分方案，但不能直接重构。

新增第三方依赖前必须说明为什么标准库不足，以及对插件安装的影响。

---

## AstrBot 与消息边界

涉及以下内容时先核对当前官方 API：

- `Star` 插件入口 / 注册
- `AstrMessageEvent`
- 消息和平台过滤器
- `event.get_group_id()`
- `event.plain_result()`
- `event.stop_event()`
- 插件配置系统 / `_conf_schema.json`
- `metadata.yaml`
- 插件市场字段
- QQ 官方适配器

这是公共插件。修改消息处理时必须明确：

- 哪些消息属于本插件
- 哪些消息回复
- 哪些消息 `stop_event()`
- 哪些消息继续交给其他插件或 LLM
- `/help` 等 AstrBot 指令是否仍正常
- 游戏中与无游戏状态是否应不同
- 配置是否改变拦截范围

不要无条件吞掉所有普通群消息，除非配置或任务明确要求独占模式。

---

## 游戏状态与玩法

状态必须按 QQ 群隔离：

- 不同 `group_id` 互不影响
- 一群操作不能改变另一群
- 开字符、猜歌、结束只作用于当前群
- 未明确要求时不引入持久化数据库

如果内存字典足够，就继续使用。

除非任务明确要求，不顺手改变：

- 开局
- 开字符
- 猜歌
- 主动 / 自动结束
- 遮罩规则
- 大小写 / Unicode 处理
- 标点匹配
- 错误提示
- 消息拦截语义

只修显示问题时不要同时改答案匹配；只改配置时不要同时重写状态管理。

---

## 曲库、配置与第三方数据

曲库优先保持：

```text
songs.txt
```

一行一个曲名。

只有实际需要别名、来源游戏、同名歌曲、多语言标题或筛选时，才规划结构升级。

曲库读取异常不能导致插件整体崩溃；不保存音频、曲绘、谱面等资源。

第三方数据：

- `LICENSE` 只描述本项目源代码许可证
- `SOURCES.md` 记录曲库 / 数据来源和许可
- 不因代码许可证推断第三方数据许可证
- 批量导入第三方数据前先检查来源和许可
- 无法确认许可时保留来源说明并列入待人工确认项

插件配置：

- 使用 AstrBot 当前官方配置机制
- 不自行实现额外 JSON 配置加载器
- 新配置必须有安全、合理的默认值
- 默认值不能让公共用户安装后意外破坏其他 AstrBot 功能
- 用户可见配置变化同步 README

---

## 文档、发布与安全

按修改类型同步检查：

| 修改 | 同步文件 |
| - | - |
| 指令 / 玩法 | `README.md` |
| 配置 | `_conf_schema.json`、`README.md` |
| 插件身份 / 仓库 | `metadata.yaml`、`README.md` |
| 版本号 | `metadata.yaml`，以及确有重复版本常量时同步 |
| 曲库来源 | `SOURCES.md` |
| 许可证 | `LICENSE`、README |
| AstrBot 兼容范围 | `metadata.yaml`、必要时 README |

未明确要求时：

- 不改版本号
- 不创建 Tag / Release
- 不改发布配置
- 不修改 `AGENTS.md`、`.gitignore`、`LICENSE`
- 不改插件内部名、作者、仓库地址
- 不改 AstrBot / QQ 部署、Docker Compose、VPS 配置
- 不改与任务无关的曲库内容

严禁提交或输出真实 AppID、AppSecret、Bot Token、SSH 密钥、API Key、VPS 密码等凭据。

发现疑似密钥时不要完整复述；提醒人工轮换，不擅自重写 Git 历史。

---

## Git 与验证

发现已有未提交修改时：

- 不覆盖
- 不回滚
- 不清理
- 有冲突时列入待人工确认项

默认不执行：

```text
git reset --hard
git clean -fd
```

也不自动 commit、push、Tag 或 Release，除非明确要求。

源码修改后至少运行：

```bash
python -m py_compile main.py
git diff --check
```

仓库如已有更完整测试，同时运行受影响测试。

按任务需要增加轻量验证：

- 指令解析
- 曲名遮罩
- Unicode / 大小写规范化
- 开字符 / 猜歌
- 群状态隔离
- 消息放行
- `stop_event()` 范围
- 配置默认值

不要为了简单断言强行新增 pytest 等依赖。

没有真实 AstrBot 环境时，不声称插件加载通过；没有 QQ 官方机器人环境时，不声称 QQ 实机测试通过。应提供人工测试步骤。

---

## 实施偏差与回复

如果实施时发现 API、文件结构或计划前提不成立，或需要：

- 明显扩大修改范围
- 新增依赖
- 改变已批准的用户可见行为

不要静默继续。说明差异、原因、推荐调整和待确认项，必要时重新进入 Plan。

### Plan 回复

至少包含：

- 已检查文件
- 当前实现结论
- 官方 API / 文档结论（如适用）
- 实施步骤
- 预计修改文件
- 验证方案
- 风险
- 待人工确认项

### Implementation 回复

至少包含：

- 修改文件和原因
- 是否按批准计划实施
- 实际验证命令和结果
- 未验证内容及原因
- 人工测试步骤
- 待人工确认项
- 是否发现计划与仓库实际不一致
