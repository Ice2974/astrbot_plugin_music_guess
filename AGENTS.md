# AGENTS.md

本文件约束 Codex / Agent 在本仓库中的协作方式、修改边界和验证要求。

项目版本、功能状态和发布信息以仓库实际文件为准。

## 信息来源与工作模式

修改前必须阅读任务相关源码、配置和文档，不根据提示词或文件名猜实现。

涉及 AstrBot、QQ 官方机器人
API、插件市场规范等变化内容时，优先查询当前官方文档或源码；有明确目标版本时核对该版本。

无法确认的实现、兼容性或验证结果列入：

`待人工确认项`

---

## Plan 阶段

提示词要求"计划阶段 / Plan Only / 只分析不修改"时：

-   只阅读、搜索、分析和制定计划
-   不修改文件
-   不 commit
-   不创建 Tag / Release
-   不执行会改写仓库的自动修复或格式化

输出：

1.  任务理解
2.  当前实现与相关文件
3.  官方 API / 文档结论（如适用）
4.  实施方案
5.  修改文件
6.  验证方案
7.  风险
8.  待确认项

收到明确实施提示词后才能修改。

---

## 项目原则

默认：

-   优先最小修改
-   Python 标准库能解决时不新增依赖
-   不提前引入数据库、Redis、ORM、后端服务或复杂分层
-   不为简单逻辑增加无意义抽象
-   未验证内容不能写成已通过

固定身份：

  标识          固定值
  ------------- --------------------------------------
  插件内部名    `astrbot_plugin_music_guess`
  显示名        `音游开字母`
  GitHub 仓库   `Ice2974/astrbot_plugin_music_guess`
  作者          `Ice2974`

---

## 架构与文件边界

核心文件：

-   `main.py`：游戏逻辑和 AstrBot 事件处理
-   `songs/`：曲库目录
-   `metadata.yaml`：插件元数据
-   `_conf_schema.json`：配置定义
-   `README.md`：用户文档
-   `LICENSE`：项目许可证
-   `.astrbot-test\AstrBot`（如果有的话）: 本地测试实例目录，同时也包含AstrBot的源码

除非任务证明必要，不新增复杂架构。

---

## 曲库规则

当前曲库：

``` text
songs/
├── phigros.txt
├── arcaea.txt
├── musedash.txt
└── ...
```

规则：

-   每个 txt 文件是一份独立曲库。
-   一行一个曲名。
-   曲库由 Agent + 人工维护，不设计自动同步。
-   不复制第三方数据库结构，只保存玩法需要的数据。

曲库加载：

-   单个曲库文件内部允许重复行，加载时完全相同的标题只保留一次。
-   多曲库同时启用时，在插件运行阶段去重。
-   只加载代码中固定列出的内置曲库文件（`BUILTIN_LIBRARY_FILES`），不扫描 `songs/` 目录。

曲库筛选：

-   不收入标题中完全不含英文或数字的曲目。
-   可遮罩字符仅限 ASCII 字母、数字与 CJK 汉字：不收入标题含日文假名（含长音符 `ー`、叠字 `々` 等假名类符号）、谚文、希腊 / 西里尔字母、带变音符拉丁字母（`é ü ǎ` 等）、全角字母、小型大写字母等键盘不便输入字符的曲目。此类字符会被遮罩，且答题需逐字精确输入，玩家实际无法作答。
-   直接显示的字符答题时同样需要逐字精确输入：不收入含常规输入法打不出同款、或形近不等价字符的曲目，如 `・`（中文键盘只能打出 `·`，两者不相等）、`♯`（易误打成 `#`）、`♭`、`†`、`〚〛`、`∅`、`∀`、`°` 等。
-   `☆`、`★`、`♪`、`♥`、箭头、几何图形等符号面板可选出的装饰符号，以及中文标点、ASCII 标点、空格允许保留。
-   保留原语言标题，不主动翻译。
-   不适合开字母玩法的纯中文、纯日文等标题应过滤。

默认启用：

-   Phigros
-   Arcaea
-   Muse Dash
-   maimai

曲库配置：

-   使用 AstrBot 官方配置机制。
-   官方名称可通过代码映射显示。

---

## AstrBot 与消息边界

涉及以下内容必须先核对官方 API：

-   插件入口 / 注册
-   `AstrMessageEvent`
-   消息过滤器
-   `event.stop_event()`
-   配置系统
-   `metadata.yaml`
-   QQ 官方适配器

修改消息处理时必须明确：

-   哪些消息属于插件
-   哪些消息回复
-   哪些消息继续交给其他插件或 LLM
-   `stop_event()` 范围

不要无条件吞掉普通群消息。

---

## 游戏状态与玩法

状态必须按 QQ 群隔离：

-   不同 `group_id` 不互相影响
-   操作只作用当前群
-   未要求时不引入持久化数据库

除非任务明确要求，不改变：

-   开局
-   开字符
-   猜歌
-   结束逻辑
-   遮罩规则
-   Unicode / 大小写处理
-   标点匹配
-   消息拦截语义

---

## 配置、文档与第三方数据

配置：

-   使用 AstrBot 官方配置机制。
-   新配置必须有合理默认值。
-   用户可见配置变化同步 README。

第三方数据：

-   不因代码许可证推断第三方数据许可证。
-   批量导入数据前检查来源和许可。
-   无法确认时列入待人工确认项。

---

## 实施规则

实施前：

-   运行 `git status --short`

实施时：

-   严格按批准计划修改
-   只修改必要文件
-   不计划外重构
-   不升级无关依赖
-   不修改发布配置

发现计划与实际仓库冲突：

-   停止扩大修改
-   报告差异
-   必要时重新进入 Plan

---

## Git、安全与发布

禁止：

-   输出真实 Token、密钥、密码
-   自动提交、push、Tag、Release（除非明确要求）
-   使用 `git reset --hard`
-   使用 `git clean -fd`

未明确要求：

-   不改版本号
-   不改插件身份
-   不修改部署架构

---

## 验证要求

源码修改后至少：

``` bash
python -m py_compile main.py
git diff --check
```

按任务补充：

-   指令解析
-   曲名遮罩
-   Unicode / 大小写规范化
-   开字符 / 猜歌
-   群状态隔离
-   消息放行
-   `stop_event()`
-   配置默认值

没有真实 AstrBot 环境时，不声称加载通过。

没有 QQ 官方机器人环境时，不声称实机测试通过。

必须说明未验证内容，并提供人工测试步骤。

---

## 实施回复

至少包含：

-   修改文件和原因
-   是否按批准计划实施
-   验证命令和结果
-   未验证内容
-   人工测试步骤
-   待人工确认项
-   是否存在计划外改动
