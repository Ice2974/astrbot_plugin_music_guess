{
  "exclusive_mode": {
    "description": "独占模式",
    "type": "bool",
    "default": false,
    "hint": "开启后，@机器人 但无法识别的消息会返回玩法提示并停止事件传播，不再进入 LLM 或其他插件；未 @机器人 的消息本插件完全不处理。适合仅用于开字母游戏的专用 Bot。默认关闭。"
  }
}
